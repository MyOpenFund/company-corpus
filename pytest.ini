[pytest]
testpaths = tests
# Network guard: every backend takes a duck-typed fetcher and the suite has always been
# network-free by convention; --disable-socket (pytest-socket) makes it a rule. A test that
# legitimately needs a socket opts back in with @pytest.mark.enable_socket (none does).
addopts = --disable-socket
